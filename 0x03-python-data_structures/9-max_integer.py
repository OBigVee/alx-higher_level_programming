#!/usr/bin/python3
def max_integer(my_list=[]):
    if len(my_list) == 0:
        return None

    t_max = 0
    
    for i in range(len(list_len)):
        if my_list[i] >= t_max:
            t_max = my_list[i]
    return t_max
