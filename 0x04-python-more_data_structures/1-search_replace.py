#!/usr/bin/python3
def search_replace(my_list, search, replace):
    _list = []
    for element in my_list:
        if search == element:
            _list.append(replace)
        else:
            _list.append(i)
    return _list
